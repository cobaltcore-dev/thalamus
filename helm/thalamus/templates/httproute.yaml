apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: model-list
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    {{- if .Values.defaultRoute.parentRefs }}
    {{- toYaml .Values.defaultRoute.parentRefs | nindent 4 }}
    {{- else }}
    - name: {{ .Values.gateway.name }}
    {{- end }}
  {{- with .Values.defaultRoute.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: Exact
            value: /v1/models
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: model-list
  namespace: {{ .Release.Namespace }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: model-list
  traffic:
    {{- if not .Values.operator.enabled }}
    directResponse:
      status: 200
      body: |
        {
          "object": "list",
          "data": [
            {{- range $i, $m := .Values.models }}
            {
              "id": {{ $m.model | quote }},
              "object": "model",
              "owned_by": {{ $m.model | splitList "/" | first | quote }}
            }{{ if lt (add1 $i) (len $.Values.models) }},{{ end }}
            {{- end }}
          ]
        }
    {{- else }}
    directResponse:
      status: 200
      body: '{"object":"list","data":[]}'
    {{- end }}
    headerModifiers:
      response:
        set:
          - name: Content-Type
            value: application/json
